window.onload = function() {

    let config = {
        width: 800,
        height: 600,
        backgroundColor: 0x000000,
        scene: [Scene_1, Scene_2]
    };

    let game = new Phaser.Game(config);



};